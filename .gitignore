# Ignore generated JSON files, but keep the credentials file explicitly tracked
*.json
!geologica-474819-ed167a0bd70c.json

# Keep TypeScript source files tracked; don't ignore *.ts / *.tsx here.
# (Previously these lines would prevent new .ts/.tsx files from being added.)
